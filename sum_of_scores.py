class Solution:
    def scoreOfString (self, s: str) -> int:
        # splut up into char arr 
        # read each char 
        ## sum i + i+1
        ## return sum of all sums 
        for i in range(4):
            currChar = ord('s')
            print(i)
        
        return -1